apiVersion: apps.qr.example.com/v1
kind: QRApp
metadata:
  labels:
    app.kubernetes.io/name: operator
    app.kubernetes.io/managed-by: kustomize
  name: qrapp-sample
spec:
  # TODO(user): Add fields here
